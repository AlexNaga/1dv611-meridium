{{> scheduleDeleteModal }}

<h3 class="subtitle is-3">Mina schemalagda arkiveringar</h3>
<table class="table is-fullwidth is-striped">
    <thead>
        <tr>
            <th>URL</th>
            <th>Sub-URL</th>
            <th>Exkludera</th>
            <th>Schemalagd</th>
            <th>Senast uppdaterad</th>
            <th class="has-text-right">Radera</th>
        </tr>
    </thead>
    <tbody class="table-row-hover">
        {{#each docs}}
            <tr title="Ändra schemaläggning" data-id="{{ _id }}">
                <td>{{friendlyUrl}}</td>
                <td>{{friendlyIncludeDomains}}</td>
                <td>{{excludePaths}}</td>
                <td>{{scheduleName}}</td>
                <td title="{{updatedAt}}">{{timeSince updatedAt}}</td>
                <td>
                    <div class="field is-grouped is-pulled-right">
                        <a href="/schedules/edit/{{_id}}"></a>
                        <button class="button is-inverted is-rounded is-danger modal-button action-delete" data-target="deleteModal" data-id="{{ _id }}"
                            title="Radera schemaläggning">
                            <span class="icon is-small">
                                <i class="fas fa-times"></i>
                            </span>
                        </button>
                    </div>
                </td>
            </tr>
        {{/each}}
    </tbody>
</table>
{{> pagination }}